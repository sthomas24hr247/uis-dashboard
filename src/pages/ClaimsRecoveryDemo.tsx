// ClaimsRecoveryDemo.tsx
// Thin wrapper — renders the full ClaimsRecoveryPage at /demo/claims
// Both routes now share identical code and features

import ClaimsRecoveryPage from "./ClaimsRecoveryPage";

export default function ClaimsRecoveryDemo() {
  return <ClaimsRecoveryPage />;
}
